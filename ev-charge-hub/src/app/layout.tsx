import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "EV Charge Hub - Smart EV Charging Management",
  description: "Book charging slots, monitor battery status, and manage your electric vehicles with ease.",
  keywords: ["EV charging", "electric vehicle", "battery monitoring", "charging station"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
